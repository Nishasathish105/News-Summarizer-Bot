services:
  - type: web
    name: news-summarizer
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y libxml2-dev libxslt-dev libjpeg-dev zlib1g-dev
      pip install -r requirements.txt
      python -m nltk.downloader punkt
    startCommand: gunicorn app:app
